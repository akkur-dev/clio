<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ======================================================== -->
    <!-- БАЗОВЫЙ СТИЛЬ ДЛЯ ПОДПИСЕЙ ПОЛЕЙ (Основной)    -->
    <!-- ======================================================== -->
    <Style x:Key="DefaultLabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="Margin" Value="10"/>
    </Style>

    <!-- ======================================================== -->
    <!-- ГЛАВНЫЙ ЗАГОЛОВОК КАРТОЧКИ / ДИАЛОГА                  -->
    <!-- ======================================================== -->
    <Style x:Key="TitleLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource DefaultLabelStyle}">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ======================================================== -->
    <!-- СЛУЖЕБНЫЙ / ТУСКЛЫЙ ТЕКСТ ПОДСКАЗОК                   -->
    <!-- ======================================================== -->
    <Style x:Key="MutedLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource DefaultLabelStyle}">
        <Setter Property="Foreground" Value="#A0A0AAB5"/>
        <Setter Property="FontWeight" Value="Light"/>
    </Style>

    <!-- ======================================================== -->
    <!-- АКЦЕНТНЫЙ ВЫДЕЛЕННЫЙ ТЕКСТ (КУРСИВ)                   -->
    <!-- ======================================================== -->
    <Style x:Key="AccentLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource DefaultLabelStyle}">
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="FontStyle" Value="Italic"/>
    </Style>

    <!-- ======================================================== -->
    <!-- КРУПНЫЙ ТЕКСТ ДЛЯ СТАТУСОВ И ЗАГЛУШЕК                 -->
    <!-- ======================================================== -->
    <Style x:Key="PlaceholderLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource DefaultLabelStyle}">
        <Setter Property="Foreground" Value="#30A0AAB5"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="Light"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

</ResourceDictionary>